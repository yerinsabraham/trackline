node_modules/
dist/
results/
.env
.env.*
!.env.example
harness.config.ts
harness.config.mts
harness.config.mjs
harness.config.js

# Local working notes. Never pushed.
documents/

# Local-only Claude Code settings (phase 0 experiment)
.claude/settings.local.json

# Built per-platform binaries; produced by scripts/build-binaries.sh at release.
packages/*/bin/

# Credentials must never sit in the working tree, least of all under docs/,
# which is published. Secrets go straight into GitHub: gh secret set NAME.
*token*.txt
*.token
.npmrc
